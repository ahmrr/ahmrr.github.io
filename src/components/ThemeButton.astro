<button
    id="theme-toggle"
    class="relative w-10 h-5 ml-2 rounded-full border transition-colors duration-300
         bg-white border-zinc-900 dark:bg-zinc-900 dark:border-white"
    aria-label="Toggle dark mode"
>
    <div
        class="absolute top-[2px] left-[2px] w-3.5 h-3.5 rounded-full bg-zinc-900 dark:bg-white
           circle-transition"
    >
    </div>
</button>

<style>
    .circle-transition {
        transform: translateX(0);
        transition: transform 0.25s ease;
    }

    html.dark .circle-transition {
        transform: translateX(20px);
    }
</style>

<script>
    const toggle = document.getElementById("theme-toggle");
    const root = document.documentElement;

    if (toggle)
        toggle.addEventListener("click", () => {
            const isDark = root.classList.toggle("dark");
            localStorage.setItem("theme", isDark ? "dark" : "light");
        });
</script>
