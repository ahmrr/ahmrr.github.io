<div class="construction-line"></div>

<style>
    .construction-line {
        position: absolute;
        bottom: 20px;
        left: 0;
        width: 100%;
        height: 15px;
        background-image: repeating-linear-gradient(
            45deg,
            #000 0,
            #000 10px,
            #ffd600 10px,
            #ffd600 20px
        );
        background-size: 28.25px 20px; /* match the stripe pattern */
        animation: move-stripes 2s linear infinite;
        z-index: 9999;
        pointer-events: none;
    }

    html.dark .construction-line {
        background-image: repeating-linear-gradient(
            45deg,
            #000 0,
            #000 10px,
            #a855f7 10px,
            #a855f7 20px
        );
    }

    @keyframes move-stripes {
        from {
            background-position: 0 0;
        }
        to {
            background-position: 28.25px 0; /* move one full pattern width */
        }
    }
</style>
